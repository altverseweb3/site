import { UserPositions } from "@/components/ui/lending/UserPositions";

export default function PositionsPage() {
  return (
    <div className="min-h-screen bg-gray-50">
      <UserPositions />
    </div>
  );
}
